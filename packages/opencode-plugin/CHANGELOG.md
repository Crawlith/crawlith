# Changelog

## 0.1.0
- Initial production build of `@crawlith/opencode-plugin`.
- Added structured OpenCode tools for crawl execution, snapshot analysis, diffs, and authority gap discovery.
- Added non-blocking OpenCode session lifecycle hooks with `client.app.log()` integration.
