{
  "name": "@crawlith/cli",
  "version": "0.1.1",
  "type": "module",
  "bin": {
    "crawlith": "./dist/index.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsup && rm -rf dist/ui && cp -r ../../plugins/web/dist ./dist/ui",
    "test": "vitest run"
  },
  "dependencies": {
    "@crawlith/core": "workspace:*",
    "chalk": "^5.3.0",
    "commander": "^12.0.0",
    "express": "^4.19.2",
    "open": "^10.1.0",
    "@crawlith/server": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^20.12.7",
    "tsup": "^8.0.0",
    "typescript": "^5.4.5",
    "vitest": "^4.0.18"
  }
}
