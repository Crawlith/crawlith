{
  "name": "@crawlith/cli",
  "version": "0.1.1",
  "type": "module",
  "bin": {
    "crawlith": "./dist/index.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "pnpm --filter @crawlith/internal-cli build && rm -rf dist && cp -r ../../plugins/cli/dist ./dist",
    "test": "pnpm --filter @crawlith/internal-cli test"
  },
  "dependencies": {
    "@crawlith/core": "workspace:*",
    "chalk": "^5.3.0",
    "commander": "^12.0.0",
    "express": "^4.19.2",
    "open": "^10.1.0"
  }
}
