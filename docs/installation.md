# Installation

## Install globally

```bash
npm install -g crawlith
```

## Run without installing

```bash
npx crawlith sitegraph https://example.com
```

## Check installed version

```bash
crawlith --version
```

> Note: If you use `npx`, you can also check version with `npx crawlith --version`.
